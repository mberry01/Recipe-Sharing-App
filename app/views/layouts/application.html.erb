<!DOCTYPE html>
<html lang="en">
<head>
  <title>Recipe Share</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>
<body class="bg-gray-100 text-gray-900 min-h-screen flex flex-col">

  <!-- Header -->
  <header class="bg-white shadow-md">
    <div class="px-6 py-4 flex justify-between items-center w-full">
      <a href="<%= main_path %>" class="text-2xl font-bold text-blue-600">Recipe Share</a>
      <nav>
        <a href="<%= profile_path %>" class="text-gray-700 hover:text-blue-600 mx-4">Profile</a>
        <a href="<%= post_path %>" class="text-gray-700 hover:text-blue-600 mx-4">+ Post</a>
        <%= link_to "Logout", session_path, method: :delete, data: { turbo_method: :delete }, class: "text-red-500 hover:text-red-700" %>
      </nav>
    </div>
  </header>

  <!-- Main Content -->
  <main class="flex-grow max-w-7xl mx-auto p-8">
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-200 text-center py-4">
    <p>&copy; <%= Time.now.year %> Recipe Share. All Rights Reserved.</p>
  </footer>

</body>
</html>
